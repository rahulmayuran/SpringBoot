input {
  kafka {
    bootstrap_servers => "localhost:9092"
    topics => ["fse_company"]
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
    index => "logstash_company-%{+YYYY.MM.dd}"
    workers => 1
  }
}